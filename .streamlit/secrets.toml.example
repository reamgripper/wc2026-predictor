# ─────────────────────────────────────────────────────────────────────────────
# EXAMPLE secrets file. Copy to ".streamlit/secrets.toml" for local testing,
# or paste the contents into the Streamlit Community Cloud "Secrets" editor.
# NEVER commit the real secrets.toml (it is .gitignored).
# ─────────────────────────────────────────────────────────────────────────────

# Optional. The admin password for full access (the "Enter" button on the
# gate page). If omitted, auth.py falls back to its built-in hashed default.
# Setting it here lets you change the password without touching the code.
admin_password = "change-me-to-your-admin-password"
